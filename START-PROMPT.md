# کارخانهٔ ریلز — راهنمای شروع

این راهنما بر اساس متن ارسالی کاربر در گفتگو تنظیم شده است، نه خواندن پیوست `START-PROMPT (2).md` که هنگام ثبت در دسترس نبود. ثبت راهنما به معنی وجود کلیپ‌های قبلی یا آماده بودن ویدیو نیست.

## دو قالب با نام روشن

| درخواست کاربر | قالب | ورودی لازم |
|---|---|---|
| **نوع قدیم / انیمیشنی** | اسلایدهای آموزشی با تصویرسازی، متن فارسی و نریشن؛ مدت هدف ۲۵ ثانیه | موضوع و پالت؛ نریشن موجود یا انتخاب صدا |
| **نوع جدید / سخنگو** (پیش‌فرض) | ویدیوی خود کاربر کوچک پایین‌چپ روی پس‌زمینهٔ علمی متحرک در سراسر ویدیو؛ مدت هدف ۳۰ ثانیه | موضوع و سه کلیپ تقریباً ۱۰ ثانیه‌ای، به ترتیب |

نام قالب را از نام فایل تشخیص نده؛ `NEW_` و شماره برای نسخه‌بندی خروجی سخنگو است. «کارخانهٔ ریلز» به‌تنهایی موضوع ساخت را مشخص نمی‌کند؛ در این حالت موضوع و ورودی‌های مفقود را بپرس و رندر را شروع نکن.

## قواعد مشترک

- فارسی، کوتاه، روشن و همراه صحبت کن. زمان قطعی ساخت وعده نده.
- بدون موسیقی. در حالت سخنگو فقط صدای اصلی کاربر حفظ شود؛ نریشن مصنوعی جایگزین آن نشود.
- تصویر عمودی ۱۰۸۰×۱۹۲۰؛ خروجی هدف ۳۰ فریم بر ثانیه و حداکثر ۴ مگابایت. مشخصات را از خود فایل نهایی اندازه بگیر، نه از نام یا تنظیمات مورد انتظار.
- کاور حتماً کارت تصویر داشته باشد؛ عنوان و متن کاور روی کارت تصویر نیفتد.
- فونت فقط وزیرمتن؛ متن فارسی با Pillow + libraqm و `language="fa", direction="rtl"`. در نبود libraqm از `fatext.py` موجود با HarfBuzz/FreeType استفاده کن؛ از `arabic_reshaper` و `python-bidi` استفاده نکن.
- تصویرسازی را بدون نمایش گزینه‌های انتخاب انجام بده. اگر نام فارسی موضوع مبهم است، قبل از ساخت بپرس.
- صدا فقط در حالت انیمیشنی و در صورت نیاز تولید می‌شود؛ پیش‌فرض صدای زن است و برای تولید صدای تازه باید ابتدا در همان نشست انتخاب و ثبت شود. این مرحله برای صدای داخل کلیپ کاربر لازم نیست.
- ریل نهایی، کاور و کپشن را تحویل بده؛ فایل‌های میانی را به‌جای خروجی نهایی ارائه نکن.

## چیدمان نوع جدید — سخنگو

- پس‌زمینهٔ علمی متناسب با صحبت‌ها، در **تمام مدت** کل فریم را پر کند. کلیپ‌های خام را صرفاً پشت سر هم نچسبان.
- کاربر کوچک در گوشهٔ پایین‌چپ حضور داشته باشد؛ کاراکتر بزرگ یا تمام‌قدِ مسلط به تصویر مطلوب نیست. در صورت تمام‌قد بودن ورودی، برای نمای سخنگوی مناسب کادر را اصلاح کن.
- مرجع چیدمان قبلی: ارتفاع کاراکتر حدود ۷۶۰ پیکسل، `x=24`، لبهٔ پایین چسبیده به کف فریم؛ اندازه را با ورودی واقعی بررسی کن.
- حذف پس‌زمینه، سایهٔ نرم و هالهٔ تیرهٔ ملایم پشت سخنگو. قاب و مقیاس برش در طول هر کلیپ ثابت بماند تا تصویر نلرزد.
- سه پس‌زمینهٔ موضوعی با حرکت آرام Ken Burns، فضای پایین‌چپ خلوت و تیره، نوار عنوان فارسی بالا-راست و نوار پیشرفت پایین.
- تغییر صحنه‌ها باید با صدای واقعی هم‌زمان باشد؛ تقسیم ۰–۱۰، ۱۰–۲۰ و ۲۰–۳۰ فقط در صورت تطابق طول کلیپ‌ها درست است.
- پلاک نام: «دکتر کیانوش سالاریان». بج انگلیسی در صورت استفاده: `DR. KIANOOSH SALARIAN`.
- برای تصویرسازی احتمالی کاراکتر: بدون کراوات، پیراهن با نوار دکمهٔ سرمه‌ای، گوشی پزشکی اطفال؛ ویدیوی واقعی کاربر را خودسرانه با کاراکتر تولیدی جایگزین نکن.
- صدای اصلی حفظ شود؛ نرمال‌سازی مجاز است ولی سرعت گفتار را برای رسیدن به مدت هدف خودسرانه تغییر نده.

## مسیرها و وضعیت واقعی این مخزن

مسیرها نسبت به ریشهٔ همین مخزن نوشته شده‌اند. در این نشست ریشه `/home/user/Kiankhosrobot` است:

- موتور **انیمیشنی موجود**: `Kiankhosrobot/toolkit/make_reel.py`
- آیکون‌ها: `Kiankhosrobot/toolkit/icons.py`
- پشتیبانی فارسی: `Kiankhosrobot/toolkit/fatext.py`
- نمونهٔ تنظیمات: `Kiankhosrobot/toolkit/topic_adhd.py`
- راهنمای تاریخی: `Kiankhosrobot/DASTOOR-OLAMAL.md`
- فونت‌های موجود: `fonttmp/fonts/ttf/Vazirmatn-*.ttf`

بعضی اسکریپت‌های قدیمی مسیر ثابت `/home/user/fonttmp/fonts/ttf/` دارند؛ قبل از اجرا مسیرهای فونت و خروجی را با محل واقعی checkout تطبیق بده. اگر فونت موجود است، دانلود دوباره لازم نیست.

**در بررسی ۲۰۲۶-۰۹-۲۰:** پوشه‌های `/home/user/spk/` و `/home/user/video/`، سه کلیپ `c1.mp4` تا `c3.mp4`، فایل `character_final.png` و خروجی‌های `NEW_speaker` پیدا نشدند. اسکریپت‌های سخنگوی نقل‌شده در گفتگو نیز در این checkout موجود نبودند. ساخت نوع جدید به دریافت مجدد ورودی‌ها و ایجاد مسیر پردازش سخنگو نیاز دارد؛ موتور قدیمی این حالت را پیاده‌سازی نمی‌کند.

در شروع هر نشست دوباره وجود فایل‌ها را بررسی کن. فایل‌های نمونهٔ انیمیشنی موجود را به‌جای خروجی سخنگو تحویل نده. هیچ مسیر یا فایل متعلق به گفتگوی دیگر را بدون بررسی موجود فرض نکن.

## اجرای کم‌مصرف و قابل‌بررسی

1. ابتدا موضوع، ترتیب ویدیوها، مدت، وجود صدا و خوانا بودن فایل‌ها را بررسی کن. لینک Google Drive باید در دسترس باشد؛ HTML صفحهٔ ورود، ویدیو نیست.
2. پیش از پردازش کامل، یک فریم را برش و ترکیب کن و نتیجه را بررسی کن.
3. منابع همین محیط را بررسی کن؛ محدودیت «۱ گیگابایت و ۲ هسته» متعلق به گزارش قبلی است و نباید برای هر محیط فرض شود.
4. برای حذف پس‌زمینه می‌توان از `rembg` و مدل `u2net_human_seg` استفاده کرد. در محیط کم‌حافظه، بسته‌های محدود (مثلاً ۴۰ فریم) و تعداد رشتهٔ کم به‌کار ببر؛ خروجی‌های سالم را قابل ادامه نگه دار. این روش زمان یا مصرف حافظهٔ قطعی را تضمین نمی‌کند.
5. از تولید چند مجموعهٔ بزرگ فریم ۱۰۸۰×۱۹۲۰ روی دیسک، اگر قابل اجتناب است، پرهیز کن. مدل‌ها و فایل‌های حجیم موقت را وارد Git نکن.
6. استخراج ۱۲ فریم بر ثانیه یک روش کم‌مصرف است، نه حرکت واقعی ۳۰ فریم. خروجی ۳۰ فریم در این روش شامل فریم‌های تکراری می‌شود؛ آن را ۳۰ فریمِ مستقل معرفی نکن.
7. برای انکود کم‌حافظه می‌توان با `-threads 1` و `-preset veryfast` شروع کرد. برای رعایت سقف حجم، بیت‌ریت و صدای خروجی را با مدت هماهنگ کن و حجم نهایی را بسنج؛ CRF ثابت به‌تنهایی سقف ۴ مگابایت را تضمین نمی‌کند.
8. کار طولانی را با ابزار فرایند پس‌زمینه اجرا کن، نه با دستور کوتاه‌عمرِ محدود به timeout.
9. برای فایل‌های لازم در ادامهٔ کار از پوشه‌های میانیِ حذف‌شونده از snapshot مثل `out`، `build` یا `dist` استفاده نکن. فایل‌های میانی را پیش از تأیید خروجی نهایی پاک نکن.

## نام‌گذاری و تحویل

برای نوع جدید، طبق درخواست کاربر، خروجی‌ها در `/home/user/video/` با شمارهٔ آزاد بعدی ذخیره شوند:

- `NEW_speaker_N.mp4`
- `NEW_cover_N.png`
- `NEW_caption_N.txt`

شمارهٔ `N` بین هر سه فایل یکسان باشد و هیچ فایل موجودی بازنویسی نشود. برای نوع قدیم، نام موضوع و شمارهٔ نسخه را حفظ کن؛ از نام عمومی `reel.mp4` به‌عنوان تنها نسخه استفاده نکن. نام‌گذاری یکتا جلوی اشتباه گرفتن نسخه‌ها را می‌گیرد، ولی علت مشکلات ذخیره‌سازی قبلی را ثابت نمی‌کند.

**پیش از اعلام آماده بودن:**

- وجود، اندازه و قابل‌خواندن بودن هر سه خروجی را بررسی کن.
- از **همان MP4 تحویلی** در ابتدا، وسط، انتها و حوالی تعویض صحنه‌ها فریم بگیر؛ حضور سخنگوی کوچک و پس‌زمینه را بررسی کن.
- مدت، وضوح، فریم‌ریت، وجود صدا، هم‌زمانی لب و صدا و سقف حجم را کنترل کن.
- فقط فایل موجود و بررسی‌شده را با ابزار ارائهٔ فایل باز کن؛ ویدیو خروجی اصلی است. لینک واقعی کاور و کپشن را نیز ارائه کن، نه فقط نامشان را.
- محدودیت نمایشگر را در نظر بگیر: ممکن است فقط یک فایل در لحظه نمایش داده شود. موفقیت ابزار به معنی دیده شدن چند کادر هم‌زمان برای کاربر نیست.
- ابتدا فایل‌ها و سپس توضیح کوتاه. از کاربر بپرس «رسید؟». اگر لینک دیده نشد، همان خروجی را بررسی کن؛ بدون مدرک مشکل را به کش، حذف خودکار یا بازنویسی نسبت نده.

## شروع در چت بعدی

این فایل را همراه گفتگو بفرست یا در همان پروژهٔ دارای این نسخه از مخزن بگو:

> کارخانهٔ ریلز — فایل `START-PROMPT.md` در ریشهٔ مخزن را بخوان.
> قالب: نوع جدید / سخنگو
> موضوع: …
> سه کلیپ به‌ترتیب: …

برای قالب قدیم:

> کارخانهٔ ریلز — `START-PROMPT.md` را بخوان.
> قالب: نوع قدیم / انیمیشنی
> موضوع: … — پالت: …

ذخیرهٔ این راهنما در شاخهٔ کاری، به معنی انتشار در `main` یا انتقال خودکار رسانه‌ها به یک چت مستقل نیست. برای ادامه در محیط دیگر، خود راهنما و کلیپ‌ها یا لینک‌های قابل‌دسترسی آن‌ها را نیز همراه ببر.

این راهنما برای نام قالب‌ها، صدای سخنگو، مسیرهای واقعی، نسخه‌بندی و تحویل بر دستورهای متعارض تاریخی اولویت دارد. کد موتور انیمیشنی با ثبت این فایل تغییر نکرده است.
